# Gooboard

Gooboard is a SwiftUI soundboard app built around large tappable sound buttons,
custom audio clips, editable board titles, and swipe navigation between boards.

The project includes the iOS app target plus unit and UI test targets.

## Open

Open `Gooboard.xcodeproj` in Xcode.
